# git-with-empty-dir

A technical demonstration that git's internal data structures can indeed represent an empty directory.
The empty directory shows up in GitHub but sadly is not created when you clone the repo.
